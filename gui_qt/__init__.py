"""GUI alternativa en PySide6 (Qt) — clon del rediseño web del simulador.

Paquete autocontenido y **paralelo** a ``gui/`` (CustomTkinter). No modifica el
motor (``modelos/``); reutiliza los puntos de entrada sin GUI de ``cli.py`` y la
configuración de ``config/``. Se lanza con ``python main_qt.py``.
"""
